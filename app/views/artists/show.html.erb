<h1> <%= @artist.name %></h1>

<% @artist.songs.each do |song| %>
  <%= render partial:"partials/song_info", locals:{song_item: song} %>
<% end %>

<%= link_to "Add Song", "/artists/#{@artist.id}/songs/new"%>
<%= link_to "Edit Artist", "/artists/#{@artist.id}/edit"%>
