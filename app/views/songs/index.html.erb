<p><%= flash[:success] %></p>

<% @songs.each do |song| %>
  <section id="song-<%= song.id %>">
    <p><%= song.title %></p>
    <p>Play Count: <%= song.play_count %></p>
    <%= button_to "Add to Cart", "/cart/#{song.id}", method: :patch %>
  </section>
<% end %>
